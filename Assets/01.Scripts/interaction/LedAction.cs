using UnityEngine;
using System.Collections;

public class LedAction : MonoBehaviour {

	/*
	void OnEnable(){
		var temp = GetComponent<TweenScale>() as TweenScale;
		Vector3 to = temp.from;
		temp.from = temp.to;
		temp.to = to;
		temp.duration = 0.1f;
		temp.Reset();
		temp.enabled = true;
	}

	void OnDisable(){
		var temp = GetComponent<TweenScale>() as TweenScale;
		Vector3 to = temp.from;
		temp.from = temp.to;
		temp.to = to;
		temp.duration = 0.005f;
		temp.Reset();
		temp.enabled = true;
	}
	*/
}
